##  Electron + React + Vite Boilerplate

`Electron` · `React 19` · `Vite 7` · `TailwindCSS` · `Zustand` · `TypeScript`

### Folder Structure

```bash
/electron   # Electron main process
/frontend   # React renderer (UI)
```

### Development

```bash
pnpm install
pnpm dev
```
